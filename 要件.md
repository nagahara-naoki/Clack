# クリックカウンター 要件定義書

最終更新: 2026-05-14

---

## 1. プロジェクト概要

毎日のキーボード打鍵数とマウスクリック数を自動でカウントし、日ごとに振り返れるシンプルなデスクトップアプリ。

**設計方針: とにかく軽量・低メモリ消費。** 24時間常駐する前提のため、リソース消費を最小限に抑えることを最優先とする。

---

## 2. 対象環境

| 項目 | 内容 |
|---|---|
| 対象OS | Windows 10 / 11、macOS 12 以降 |
| アーキテクチャ | x64 / arm64 |
| ネットワーク | 不要（完全オフライン動作） |

---

## 3. 機能要件

### 3.1 計数

- グローバルでキーボード打鍵数とマウスクリック数を取得（バックグラウンド）
- キー数とマウス数は **別カウント** で保存
- **長押し（オートリピート）は無効**：キー押下中に発生する連続イベントは1回としてカウント
  - 実装方針: 押下中のキー集合を保持し、キーダウン時に未登録のキーのみカウント
- 押された「キーの種類」は一切記録しない（数のみ）
- マウスはボタンクリック（左 / 中 / 右）のみ対象。スクロール・カーソル移動はカウント対象外

### 3.2 アイドル除外

- 一定時間（既定 60 秒）入力イベントがなければアイドル状態と判定
- アイドル中はカウントを一時停止し、OS 由来の自動イベント混入を防ぐ
- 新たな入力で自動復帰

### 3.3 データ保存

- ローカルの JSON ファイルへ保存
- 保存先（OS 標準のユーザーデータディレクトリ）:
  - Windows: `%APPDATA%\ClickCounter\data.json`
  - macOS: `~/Library/Application Support/ClickCounter/data.json`
- データ構造:

```json
{
  "2026-05-14": { "keys": 12345, "mouse": 678 },
  "2026-05-13": { "keys": 9876,  "mouse": 543 }
}
```

- I/O 削減のためメモリ上で集計し、一定間隔（例: 60 秒ごと）でバッチ書き込み
- アプリ終了時・日付変更時には即時フラッシュ

### 3.4 閲覧 UI

トグルで切替できる 2 つのビューを用意する。

**カレンダービュー（ヒートマップ）**
- 月単位のグリッド表示
- 日ごとの合計値を色の濃さで表現
- 前月 / 翌月ナビゲーション
- ホバー時に詳細値をツールチップ表示

**リストビュー**
- 日付・キー数・マウス数の縦並び表
- 新しい日付順
- スクロール式

### 3.5 常駐動作

- システムトレイ（Windows）／メニューバー（macOS）に常駐
- トレイアイコンクリックで閲覧ウィンドウを開閉
- 右クリックメニュー: 開く / 自動起動切替 / 終了
- 閲覧ウィンドウを閉じてもバックグラウンドで計数を継続

### 3.6 自動起動

- **デフォルト ON**（OS 起動時に自動で起動）
- 設定画面から ON / OFF を切替可能

### 3.7 設定項目（最小限）

- 自動起動 ON / OFF
- アイドル判定の閾値（既定 60 秒）

---

## 4. 非機能要件

### 4.1 リソース消費（マスト要件）

| 項目 | 目標値 |
|---|---|
| アイドル時メモリ使用量 | 50 MB 以下 |
| アイドル時 CPU 使用率 | 1 % 未満 |
| アプリ本体サイズ | 20 MB 以下 |

### 4.2 プライバシー

- 完全オフライン、外部通信を一切行わない
- キー種類・押下内容は記録しない
- データはユーザー領域に平文保存（個人利用前提）

### 4.3 信頼性

- アプリクラッシュ時もバッチ書込済分までの計数はディスクに残る
- 日付変更時に集計区切りが正しく切り替わる
- スリープ復帰後も計数を継続

---

## 5. 技術スタック

| レイヤ | 選定 |
|---|---|
| アプリフレームワーク | **Tauri 2.x** |
| バックエンド言語 | Rust |
| グローバル入力フック | `rdev` クレート |
| フロントエンド | 素の HTML / CSS / JavaScript（フレームワーク非採用） |
| データ保存 | JSON ファイル（`serde_json`） |

**選定理由**:
Electron は Chromium を内包するため、常駐用途では重すぎる。Tauri は OS 標準の WebView を利用するため、サイズ・メモリ消費ともに桁違いに軽い。フロントエンドも React 等を使わず素の HTML/CSS/JS にすることでバンドルサイズを最小化する。

---

## 6. 画面構成

**メインウィンドウ（閲覧用）**
- 上部: カレンダー／リスト切替トグル
- 中央: 選択中のビュー本体
- フッター: 設定アイコン、当月合計値

**設定ウィンドウ（モーダル）**
- 自動起動チェックボックス
- アイドル閾値入力

**システムトレイ**
- 左クリック: メインウィンドウ開閉
- 右クリック: コンテキストメニュー

---

## 7. 配布・パッケージング

- Windows: MSI インストーラ
- macOS: DMG パッケージ
- macOS 初回起動時にアクセシビリティ権限の許可フローを案内
- コード署名・公証は将来対応（初版ではオプション扱い）

---

## 8. やらないこと（非対象）

明確にスコープ外とするもの:

- キー種類・押下内容の記録
- 入力傾向や利用パターンの分析
- WPM・単語数の計測
- アプリ別 / ウィンドウ別の集計
- クラウド同期・複数デバイス連携
- データエクスポート機能（初版では不要、将来検討）
- スクロールやマウス移動量の記録

---

## 9. 想定リスク

| リスク | 対策 |
|---|---|
| macOS でアクセシビリティ権限が拒否されると計数不可 | 初回起動時に明確な案内画面を表示 |
| アンチウィルスがグローバルキーフックを誤検知する可能性 | 配布ドキュメントで言及、署名で軽減 |
| 特殊キーボード（ゲーミング等）で `rdev` がイベントを拾えない可能性 | 開発初期に検証 |
| 自動起動の OS 別仕様差（特に macOS の Login Items） | Tauri のオートスタートプラグインで吸収 |
