{
  "name": "clack",
  "version": "0.1.0",
  "private": true,
  "description": "Clack — lightweight keyboard and mouse activity counter (Tauri 2 + vanilla JS).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node scripts/build.js",
    "build:exe": "node scripts/build.js --no-bundle",
    "collect": "node scripts/copy-artifacts.js",
    "icons": "tauri icon icons/icon-1024.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1",
    "http-server": "^14.1.1"
  }
}
